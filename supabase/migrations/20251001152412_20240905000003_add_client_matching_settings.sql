/*
  # Add client matching settings to webhook configuration
  
  1. Changes
    - Settings for how to match incoming checkins to existing clients
    - Already part of user_checkin_webhook_settings table
    
  2. Notes
    - This migration is a placeholder as settings already exist in the base table
*/

-- Settings already exist in user_checkin_webhook_settings table
-- This migration ensures the structure is documented

SELECT 1; -- No-op migration, structure already exists
